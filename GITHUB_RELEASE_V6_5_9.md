# GitHub — V6.5.9 Navigation & lecture

Cette archive remplace directement la V6.5.8 à la racine du dépôt. Aucun build frontend n'est requis.

```bash
git add -A
git commit -m "Carnet Peche JP V6.5.9 navigation lecture"
git push
```

Après GitHub Pages, effectuer un rechargement complet une fois puis fermer/réouvrir la PWA pour activer le cache `carnet-peche-jp-v6-5-9-navigation-lecture-20260812`.

Contrôle téléphone recommandé :

1. ouvrir Ise-Shima ou Kashima et vérifier que `Lecture destination`, `Matériel`, `Preuves & contraintes` et `Comprendre le créneau` s'ouvrent/se referment correctement ;
2. ouvrir une fiche espèce, scroller puis passer de `Terrain` à `Animations` et `Comprendre` ;
3. vérifier que la barre d'onglets reste collée juste sous le header sans masquer le contenu.
