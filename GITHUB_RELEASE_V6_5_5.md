# GitHub — V6.5.5 Enrichissement comportemental

Le dossier de release peut être copié directement à la racine du dépôt GitHub Pages.
Aucune étape de build n'est nécessaire.

```bash
git add -A
git commit -m "Carnet Peche JP V6.5.5 behavioral enrichment"
git push
```

Le service worker utilise le cache :

`carnet-peche-jp-v6-5-5-behavioral-enrichment-20260812`

Après le déploiement, ouvrir une fois l'application avec du réseau afin que le nouveau service worker et les JSON soient mis en cache.

Release source de vérité : `PROJECT_STATE.md` + `QA_V6_5_5_BEHAVIORAL_ENRICHMENT.md`.
