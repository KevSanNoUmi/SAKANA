# GitHub — V6.5.8 UX focus mobile

Cette archive est prête à remplacer le contenu de la release précédente à la racine du dépôt.

Aucun build frontend n'est requis. Node.js reste requis uniquement lorsqu'un futur `python pipeline.py export` doit régénérer `app_core.json` et `decision_cache.json`.

```bash
git add -A
git commit -m "Carnet Peche JP V6.5.8 UX focus mobile"
git push
```

Après GitHub Pages, faire un rechargement complet une fois, fermer puis rouvrir la PWA afin d'activer le cache `carnet-peche-jp-v6-5-8-ux-focus-20260812`.

Contrôle rapide : ouvrir un pad multi-espèces, toucher une bande une fois (sélection), puis une deuxième fois (plan détaillé). Vérifier également que les anciennes mentions `n=…` ne sont plus visibles.
